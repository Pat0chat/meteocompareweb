name: Roll back GitHub Pages

on:
  workflow_dispatch:
    inputs:
      tag:
        description: 'Stable release tag to redeploy (example: v1.9.0)'
        required: true
        type: string

permissions:
  contents: read
  pages: write
  id-token: write
  actions: read

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  validate-and-package:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout requested release
        uses: actions/checkout@v5
        with:
          ref: ${{ inputs.tag }}
          fetch-depth: 0
      - name: Remove obsolete runtime residue
        shell: bash
        run: rm -f js/android_strings.js

      - name: Validate release tag
        shell: bash
        run: |
          set -euo pipefail
          git describe --tags --exact-match HEAD
          test "v$(tr -d '\r\n' < VERSION)" = "${{ inputs.tag }}"
      - name: Run regression tests from release
        run: |
          set -e
          for test in tests/*.mjs; do node "$test"; done
      - name: Configure Pages
        uses: actions/configure-pages@v6
      - name: Prepare release site
        run: |
          mkdir -p _site
          rsync -a --exclude '.git' --exclude '.github' --exclude '_site' --exclude 'tests' ./ _site/
      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v5.0.0
        with:
          path: _site
          include-hidden-files: true
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: validate-and-package
    steps:
      - name: Deploy selected stable release
        id: deployment
        uses: actions/deploy-pages@v5.0.0
